# Normalize all text files to LF in the repository (cross-platform scripts).
* text=auto eol=lf

# Keep Windows batch files as CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary assets.
*.png binary
